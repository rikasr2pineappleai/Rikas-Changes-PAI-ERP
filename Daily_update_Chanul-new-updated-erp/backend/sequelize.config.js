// sequelize.config.js - Centralized Sequelize CLI configuration
// This ensures consistency between runtime and migration configurations

const config = require('./config/database.js');

module.exports = config;